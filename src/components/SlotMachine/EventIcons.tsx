
import React from 'react';
import { motion } from 'framer-motion';


interface EventIconsProps {
  onEventClick: (eventId: string) => void;
}

const EventIcons: React.FC<EventIconsProps> = ({ onEventClick }) => {
  return null;
};

export default EventIcons;
